stages:
    - build

before_script:
  - nuget restore -NonInteractive
  
job:
    stage: build
    script:
    # The output path is relative to the position of the csproj-file
    - msbuild /p:Configuration="Debug" /p:Platform="Any CPU"
      /p:OutputPath="./../obj\Debug/" "NationalFundingDev.sln"